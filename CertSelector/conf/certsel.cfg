MTLS|SRVR_TLS,OPERFS_P12,P12,file:///<path to p12 file>,<authentication config for p12>
MTLS,OPERFB_P12,P12,file:///etc/ssl/certs/<path to static fallback p12 file>,<authentication config for fallback p12>
MTLS_PEM,OPERFS_PEM,PEM,file:///<path to pem1>,< config for fallback pem1>
MTLS_PEM,OPERFB_PEM,PEM,file:///<path fallback pem2>,< config for fallback pem2>
RCVRY,REDRCVRY,P12,file:///<path to long term recovery fall back p12 cert>,< authentication config for p12>
